{
  "name": "formation-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "tsc": "npx tsc",
    "tsc:watch": "npx tsc exercices/*.ts --watch --rootDir exercices --outDir dist",
    "tsc:watch:strict": "npx tsc exercices/*.ts --watch --rootDir exercices --outDir dist --noEmitOnError --strict"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/tsconfig": "^0.7.0",
    "typescript": "~5.7.2",
    "vite": "^6.1.0",
    "vue-tsc": "^2.2.0"
  }
}
