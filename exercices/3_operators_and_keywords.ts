// Keyword AS: Permet de considérer le typage comme celui défini

// As const: Permet de rendre un élément immuable
const routes = ['home', 'about', 'annonce']
routes.push("route qui n'existe pas")

// Narrowing: TS déduit le typage par l'éliminations de cas 
